var drawPoint = function (point) {
    console.log("Draw point at X: " + point.x + " & Y: " + point.y);
};
var drawPointX = function (point) {
    console.log("Draw point at X: " + point.x + " & Y: " + point.y);
};
drawPointX({
    x: 100,
    y: 2
});
